/* SprawdŸ, ile bajtów pamiêci zajmuje zmienna wskaŸnikowa. */
#include<stdio.h>
main()
{
    int *p;
    printf("Wskaznik zajmuje %d bajty \n", sizeof(p));
    system("PAUSE");
    return 0;
}
